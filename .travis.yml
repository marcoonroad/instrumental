sudo: required
language: node_js

env:
  - BUILD_JOB_TYPE=test
  - BUILD_JOB_TYPE=report

node_js:
  - "8"

before_install:
  - export PATH=./node_modules/.bin:$PATH

script:
  - "yarn run $BUILD_JOB_TYPE"

cache:
  yarn: true
  directories:
    - "node_modules"
